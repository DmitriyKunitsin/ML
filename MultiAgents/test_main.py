"""Фасад-заглушка (обратная совместимость).

Исходный код пайплайна переехал в ``main.py`` — это и есть точка входа
(``python main.py``). Этот модуль сохраняет старое имя ``test_main`` для
тестов и сторонних импортов: все атрибуты ``main`` реэкспортируются сюда
включая приватные (``_should_rewrite_from_scratch`` и т.п.).

Правило на будущее: импортируйте из ``main``, а не из этого фасада.
"""

import sys as _sys

import main as _main

# Полный реэкспорт: переносим ВСЁ пространство имён main (в т.ч. _-имена,
# логгер, константы) в этот модуль.
_sys.modules[__name__].__dict__.update(_main.__dict__)