ARDUINO_PROMPT = """
Твоя задача — разработать полноценное, структурированное и образовательное задание для Junior-разработчика в области Arduino.

Задание должно быть исключительно программным и не требовать подключения дополнительного оборудования, кроме стандартной платы Arduino (Uno/Nano/Mega) и встроенных компонентов (светодиод на пине 13, кнопка сброса, последовательный порт).

---

## 📋 ОБЩИЕ ТРЕБОВАНИЯ К ЗАДАНИЮ

1. **Сложность:** Задание должно быть рассчитано на Junior-разработчика (0–1 год опыта с Arduino).
2. **Объём кода:** 150–300 строк чистого C++ кода (без учёта комментариев).
3. **Формат:** Полный скетч (.ino), готовый к компиляции и загрузке.
4. **Документация:** В коде должны быть подробные комментарии на русском языке (не менее 30% от общего объёма).
5. **Обучающая ценность:** Задание должно научить Junior-разработчика следующим навыкам:
   - Работа с цифровыми и аналоговыми пинами.
   - Использование прерываний и таймеров.
   - Написание неблокирующего кода (без delay()).
   - Работа с последовательным портом (Serial).
   - Обработка нажатий кнопок с антидребезгом.
   - Использование конечных автоматов (state machines).
   - Работа с библиотеками (например, LiquidCrystal_I2C, EEPROM, TimerOne).

---

## 🎯 ОПИСАНИЕ ЗАДАЧИ (ОСНОВНАЯ ИДЕЯ)

Разработать программу для Arduino, которая реализует **интерактивный тренажёр-симулятор "Умный светофор"** с элементами геймификации.

Программа должна имитировать работу светофора с пешеходным режимом, используя:
- Встроенный светодиод (пин 13) как индикатор состояния.
- Последовательный порт (Serial) для взаимодействия с пользователем.
- Внутренний таймер для отсчёта времени.
- Кнопку сброса для перезапуска цикла.

---

## 🧩 МОДУЛИ ЗАДАНИЯ (ЧТО ДОЛЖЕН РЕАЛИЗОВАТЬ JUNIOR)

### Модуль 1. Конечный автомат светофора (State Machine)

Реализовать конечный автомат с 5 состояниями:
1. **IDLE** — ожидание запуска (мигание светодиода с частотой 1 Гц).
2. **GREEN** — зелёный свет (горит 10 секунд).
3. **YELLOW** — жёлтый свет (горит 3 секунды).
4. **RED** — красный свет (горит 7 секунд).
5. **PEDESTRIAN** — пешеходный режим (включается по команде из Serial).

Переходы между состояниями должны происходить:
- Автоматически по таймеру (для основных состояний).
- По команде из Serial (для включения пешеходного режима).

---

### Модуль 2. Неблокирующий таймер (Non-blocking Timer)

Запрещено использовать `delay()`.
Вместо этого использовать **миллис-таймер** (`millis()`) для управления временем в каждом состоянии.

Требования:
- Таймер должен быть переиспользуемым (одна функция для всех состояний).
- Точность: ±10 мс.
- Должен корректно обрабатывать переполнение `millis()` (через 49 дней).

---

### Модуль 3. Обработка команд из Serial

Реализовать обработку текстовых команд, поступающих из последовательного порта:
- `start` — запустить цикл светофора (переход из IDLE в GREEN).
- `stop` — остановить цикл (переход в IDLE).
- `pedestrian` — включить пешеходный режим (переход в PEDESTRIAN, если светофор активен).
- `status` — вывести текущее состояние и оставшееся время.

Все команды должны быть регистронезависимыми.

---

### Модуль 4. Визуализация через Serial

Выводить в Serial-монитор:
- Текущее состояние светофора (GREEN, YELLOW, RED, PEDESTRIAN, IDLE).
- Оставшееся время в текущем состоянии (в секундах).
- События (например, "Пешеходный режим активирован", "Цикл запущен").

Формат вывода должен быть читаемым и структурированным (с отступами и разделителями).

---

### Модуль 5. Логирование и сохранение состояния (опционально, для продвинутых Junior)

Использовать встроенную EEPROM для сохранения:
- Количества запусков светофора.
- Общего времени работы в каждом состоянии.

При старте программы выводить эту статистику в Serial.

---

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ (ДЛЯ ПРОВЕРКИ РАБОТЫ)

Junior-разработчик должен убедиться, что программа корректно работает в следующих сценариях:

1. **Запуск:** После загрузки программы светодиод мигает 1 Гц (состояние IDLE).
2. **Команда start:** Светодиод переходит в режим GREEN (горит 10 секунд).
3. **Автоматический переход:** После 10 секунд GREEN → YELLOW (3 секунды) → RED (7 секунд) → GREEN (цикл повторяется).
4. **Команда pedestrian:** Если светофор активен (не в IDLE), он переходит в PEDESTRIAN на 5 секунд, затем возвращается в состояние, в котором был прерван.
5. **Команда stop:** В любом состоянии возвращает в IDLE.
6. **Команда status:** Выводит текущее состояние и оставшееся время.
7. **Переполнение таймера:** Программа должна корректно работать более 49 дней без сбоев.

---

## 📚 ТРЕБОВАНИЯ ПО КОД-СТАЙЛУ (ЧЕМУ НАУЧИТЬ JUNIOR)

1. **Именование:**
   - Переменные и функции — `camelCase`.
   - Константы и макросы — `UPPER_SNAKE_CASE`.
   - Типы данных (enum, struct) — `PascalCase`.

2. **Комментарии:**
   - Каждая функция должна иметь комментарий с описанием, параметрами и возвращаемым значением.
   - Сложные участки кода (таймер, конечный автомат) должны быть подробно прокомментированы.
   - Комментарии на русском языке, без орфографических ошибок.

3. **Структура:**
   - Разделить код на логические блоки: константы, глобальные переменные, функции, `setup()`, `loop()`.
   - Использовать `enum` для состояний и `struct` для хранения параметров таймера.

4. **Обработка ошибок:**
   - Проверять входящие данные из Serial на корректность.
   - Предусмотреть защиту от невалидных команд.

---

## 📤 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

На выходе должен быть:
1. **Полный скетч (.ino)** — готовый к компиляции и загрузке.
2. **Блок-схема конечного автомата** (в виде ASCII-графики или текстового описания).
3. **Инструкция по использованию** — список команд для Serial-монитора.
4. **Файл с тестами** — описание, как проверить каждый сценарий.

---

## 🛠️ ДОПОЛНИТЕЛЬНЫЕ ИНСТРУКЦИИ ДЛЯ JUNIOR

- Используй только стандартные библиотеки Arduino.
- Если требуется сторонняя библиотека, укажи её название и версию.
- Код должен быть кросс-платформенным (работать на Uno, Nano, Mega).
- Размер скетча не должен превышать 30 КБ (чтобы поместился в память Uno).

---

## 🎯 КРИТЕРИИ УСПЕШНОГО ВЫПОЛНЕНИЯ

- ✅ Код компилируется без ошибок и предупреждений.
- ✅ Все команды из Serial обрабатываются корректно.
- ✅ Светодиод мигает в соответствии с состояниями.
- ✅ Таймер работает без `delay()`.
- ✅ Код соответствует требованиям по код-стайлу.
- ✅ Комментарии покрывают не менее 30% кода.
- ✅ Есть обработка переполнения `millis()`.

---

## 💡 ПОЛЕЗНЫЕ СОВЕТЫ ДЛЯ JUNIOR

- Используй `enum` для состояний — это сделает код читаемее.
- Для таймера создай структуру `Timer`, которая хранит `startTime`, `interval` и флаг `active`.
- Для обработки команд из Serial используй `strcmp()` или сравнение строк через `strcasecmp()`.
- Для антидребезга кнопки используй тот же миллис-таймер (проверяй состояние с задержкой 50 мс).
- Логируй все события в Serial, чтобы легче было отлаживать.

---

## 🧠 РЕФЛЕКСИЯ (ЧТО ДОЛЖЕН ПОНЯТЬ JUNIOR ПОСЛЕ ВЫПОЛНЕНИЯ)

После выполнения этого задания Junior-разработчик должен понять:
- Как проектировать конечные автоматы на Arduino.
- Как использовать `millis()` для неблокирующих задержек.
- Как обрабатывать текстовые команды через Serial.
- Как структурировать код для Arduino-проектов.
- Как отлаживать программу через Serial-монитор.
- Как работать с переполнением таймера и предотвращать ошибки.

---

## 🚀 ЗАКЛЮЧИТЕЛЬНАЯ ЧАСТЬ

Задание должно быть представлено в виде структурированного документа, включающего:

1. **Описание проекта** (введение, цель, требования).
2. **Техническое задание** (функциональные и нефункциональные требования).
3. **Блок-схема** (визуализация конечного автомата).
4. **Инструкция по установке и использованию** (как загрузить код, какие команды вводить).
5. **Примеры работы** (скриншоты Serial-монитора).
6. **Код** (полный скетч с комментариями).
7. **Тесты** (перечень проверок для каждого сценария).
8. **Заключение** (чему научились, что можно улучшить).

---

**Это задание даст Junior-разработчику не просто "написать код", а пройти полный цикл разработки встраиваемого ПО: от ТЗ до тестирования.** 🚀


"""
